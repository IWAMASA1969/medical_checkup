import React from 'react';
import PropTypes from 'prop-types';
import Button from '@material-ui/core/Button';

class ButtonAddStdValue extends React.Component {
  render () {
    return (
      <div>
        standard value table
        <Button>
          add
        </Button>
      </div>
    );
  }
}

export default ButtonAddStdValue
